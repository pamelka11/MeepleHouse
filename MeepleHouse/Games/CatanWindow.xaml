<Window x:Class="MeepleHouse.Games.CatanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Колонизаторы"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F2F2F2">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <TextBlock
Text="Колонизаторы"
FontSize="28"
FontWeight="Bold"
HorizontalAlignment="Center"
VerticalAlignment="Center"/>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- место под картинку -->
            <Border Grid.Column="0" CornerRadius="10" Margin="0 0 20 0">
                <Image Source="Image\Catan.png" Stretch="Fill"/>
            </Border>

            <StackPanel Grid.Column="1">

                <TextBlock
Text="Экономическая стратегия, где игроки строят поселения и развивают остров Катан."
FontSize="15"
TextWrapping="Wrap"
Margin="0 10 0 20"/>
                <TextBlock x:Name="DateText"
           FontSize="14"
           Margin="0 0 0 10"/>
                <TextBlock
x:Name="PlayersText"
FontSize="16"
FontWeight="Bold"/>

            </StackPanel>

        </Grid>

        <Button
Grid.Row="2"
Content="Записаться"
Width="200"
Height="40"
HorizontalAlignment="Right"
VerticalAlignment="Center"
Background="#4CAF50"
Foreground="White"
Click="Register_Click"/>

    </Grid>
</Window>