<Window x:Class="MeepleHouse.Games.TicketToRideWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Ticket to Ride"
Height="450"
Width="800"
WindowStartupLocation="CenterScreen"
Background="#F2F2F2">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <TextBlock
Text="Ticket to Ride"
FontSize="28"
FontWeight="Bold"
HorizontalAlignment="Center"
VerticalAlignment="Center"/>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="LightGray" CornerRadius="10" Margin="0 0 20 0"/>

            <StackPanel Grid.Column="1">

                <TextBlock
Text="Игра про строительство железнодорожных маршрутов между городами."
FontSize="15"
TextWrapping="Wrap"
Margin="0 10 0 20"/>

                <TextBlock x:Name="PlayersText" FontSize="16" FontWeight="Bold"/>

            </StackPanel>

        </Grid>

        <Button
Grid.Row="2"
Content="Записаться"
Width="200"
Height="40"
HorizontalAlignment="Right"
Background="#4CAF50"
Foreground="White"
Click="Join_Click"/>

    </Grid>
</Window>